class SceneWizardDeviceClass {
    constructor() {
    }
    // add event listener on each device item.
    // render device properties page
    // handle property touch to open correspond modal.

    // after press next button, we should call "scene-wizard" class method to add config to previous config variable.
    // for that we need to identify all datapoint that selected by user and added them to config.
}

export {SceneWizardDeviceClass}