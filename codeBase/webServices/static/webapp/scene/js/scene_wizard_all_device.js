class SceneWizardAllDevicesPageClass {
    constructor() {
    }
    // list of all device classes.
    // remove all device classes list.
    // render all device page.

    // create device classes list each time show page and render based on them, because if we didn't deconstruct
    // device list classes, device function values have value from last selection and it's not our desire.

}


export {SceneWizardAllDevicesPageClass}